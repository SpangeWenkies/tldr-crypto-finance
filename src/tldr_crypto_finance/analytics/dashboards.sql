SELECT * FROM v_topic_counts_by_week ORDER BY week_start DESC, topic;
SELECT * FROM v_domain_counts LIMIT 20;
SELECT * FROM v_watchlist_hits ORDER BY sent_at DESC;
SELECT * FROM v_asset_risk_slices;
SELECT * FROM v_parse_quality_by_newsletter;
